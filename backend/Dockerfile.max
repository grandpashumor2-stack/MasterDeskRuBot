FROM python:3.12-slim
WORKDIR /app
COPY requirements-max.txt .
RUN pip install --no-cache-dir -r requirements-max.txt
COPY . .
RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
USER appuser
CMD ["python", "scripts/max_bot_manager.py"]
